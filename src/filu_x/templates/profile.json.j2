{
  "$schema": "https://filu-x.org/schemas/profile-v{{ version }}.json",
  "version": "{{ version }}",
  "pubkey": "{{ pubkey }}",
  "display_name": "@{{ username }}",
  "display_name_normalized": "{{ username | lower }}",
  "author": "@{{ username }}",
  "profile_ipns": "{{ profile_ipns }}",
  "manifest_ipns": "{{ manifest_ipns }}",
  "threads_ipns": {{ threads_ipns|default({})|tojson }},  {# Map of thread_id -> IPNS #}
  "bio": "",
  "avatar_url": "",
  "created_at": "{{ now_iso8601 }}",
  "updated_at": "{{ now_iso8601 }}",
  "access_points": [],
  "signature": "{{ signature }}"
}
